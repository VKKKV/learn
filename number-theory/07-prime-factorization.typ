#import "@local/math-note:0.1.0": *
#show: note-template

#set document(title: "分解质因数", author: "vkkkv")

= 分解质因数

质因数分解把整数转成“素数指数向量”的形式。
一旦完成分解，很多数论函数都能直接写出闭式公式，因此它是初等数论计算中最重要的预处理之一。

== 算术基本定理

#theorem[
  任意整数 $n > 1$ 都能唯一地写成
  $ n = p_1^(a_1) p_2^(a_2) dots.c p_k^(a_k) $
  其中 $p_1, p_2, dots.c, p_k$ 两两不同且都是素数。
]

#remark[
  “唯一”是指素因子和对应指数在重排意义下唯一。
  这使得很多关于约数、互素和积性函数的结论都能逐个素因子处理。
]

== 由分解得到的信息

#proposition[
  已知
  $ n = p_1^(a_1) p_2^(a_2) dots.c p_k^(a_k) $
  后，可以立刻得到：

  - 约数个数
    $ d(n) = (a_1 + 1)(a_2 + 1) dots.c (a_k + 1) $
  - 约数和
    $ sigma(n) = (1 + p_1 + dots.c + p_1^(a_1)) dots.c (1 + p_k + dots.c + p_k^(a_k)) $
  - 欧拉函数
    $ phi(n) = n (1 - 1/p_1) dots.c (1 - 1/p_k) $
]

#proof[
  约数由每个素因子的指数独立选择：
  $0, 1, 2, dots.c, a_i$
  所以约数个数等于各自选择数目的乘积。

  约数和与欧拉函数同理，都是将“每个素因子的局部贡献”乘起来。
]

== 试除法

#proposition[
  对一个正整数 $n$ 做试除分解时，只需枚举到
  $ sqrt(n) $
  即可。
]

#proof[
  若 $n$ 是合数，则可写成
  $ n = a b $
  其中至少有一个因子不超过 $sqrt(n)$。
  因而如果直到 $sqrt(n)$ 都没有找到非平凡因子，剩下的数只能是 $1$ 或素数。
]

== 例题

#example[
  将
  $ 72 $
  分解质因数：

  $ 72 = 2^3 3^2 $

  所以它的约数个数为
  $ (3 + 1)(2 + 1) = 12 $
]

#example[
  求
  $ sigma(18) $

  因为
  $ 18 = 2 3^2 $
  所以
  $ sigma(18) = (1 + 2)(1 + 3 + 9) = 39 $
]

== 与算法的关系

#remark[
  当 $n$ 较小时，试除法足够；
  当需要批量处理很多整数时，常配合最小质因子筛；
  当整数非常大时，则需要更高级的分解算法。
]

#exercise[
  分解
  $ 360 $
  并求它的约数个数与欧拉函数值。
]
