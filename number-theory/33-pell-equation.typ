#import "@local/math-note:0.1.0": *
#show: note-template

#set document(title: "Pell 方程", author: "vkkkv")

= Pell 方程

Pell 方程的标准形式是
$ x^2 - d y^2 = 1 $
其中 $d$ 为非平方正整数。
它看上去只是一个简单的二元二次方程，但解集拥有很深的结构，并与连分数和二次域紧密相连。

== 方程形式

#definition[
  当 $d$ 为非平方正整数时，方程
  $ x^2 - d y^2 = 1 $ <pell>
  称为 Pell 方程。
]

== 基本结论

#theorem[
  当 $d$ 不是平方数时，Pell 方程有无穷多组整数解。
  其基本解通常可由
  $ sqrt(d) $
  的连分数展开求出。
]

#remark[
  这一点非常反直觉：
  一个具体的二元方程，不仅有解，而且通常会有无限多组解。
]

== 基本解与递推

#definition[
  使得 $x > 0$、$y > 0$ 且 $x$ 最小的一组解，通常称为基本解或最小正解。
]

#proposition[
  一旦找到最小正解，其他解通常可以通过二次域中的乘法结构递推得到。
]

#remark[
  更具体地说，若
  $ x_1 + y_1 sqrt(d) $
  是基本单位，则更高次幂
  $ (x_1 + y_1 sqrt(d))^k $
  会产生后续解。
]

== 例子

#example[
  当
  $ d = 2 $
  时，方程
  $ x^2 - 2 y^2 = 1 $
  的最小正解是
  $ (x, y) = (3, 2) $
  因为
  $ 3^2 - 2 2^2 = 9 - 8 = 1 $
]

== 与连分数的关系

#remark[
  求解 Pell 方程最经典的方法是：

  - 先求
    $ sqrt(d) $
    的连分数展开
  - 找到其周期
  - 再由周期信息恢复最小正解

  这也是为什么 Pell 方程常被视作“连分数理论最漂亮的应用之一”。
]

#exercise[
  验证
  $ (3, 2) $
  确实是
  $ x^2 - 2 y^2 = 1 $
  的解。
]
