#import "@local/math-note:0.1.0": *
#show: note-template

#set document(title: "Min_25 筛", author: "vkkkv")

= Min_25 筛

Min_25 筛是一类用于求质数相关前缀和的高级算法，常见于
$ sum f(p) $
以及积性函数前缀和问题。
它的难点不在某一条公式，而在于如何把“按质数筛去贡献”的过程组织成有限状态递推。

== 适用场景

#remark[
  Min_25 筛通常用于：

  - 求质数贡献前缀和
  - 求某些积性函数前缀和
  - 数据规模远大于线性筛能承受的范围
]

== 核心思想

#proposition[
  它通常利用：

  - 小于等于 $sqrt(n)$ 的素数预处理
  - 对 $floor(n / i)$ 的不同取值建状态
  - 把“筛掉前若干个素数”的过程写成递推
]

#remark[
  从结构上看，Min_25 筛有点像“把筛法本身递归化”。
  不是逐个数去标记，而是逐块地维护“剩余未被小素数筛掉的总贡献”。
]

== 为什么状态数不大

#proposition[
  由于
  $ floor(n / i) $
  的不同取值个数只有 $O(sqrt(n))$ 级别，因此状态数组规模远小于 $n$ 本身。
]

#remark[
  这一步和数论分块的思想完全一致，是 Min_25 筛能成立的关键。
]

== 学习前置

#remark[
  建议先熟悉：

  - 线性筛
  - 数论分块
  - 杜教筛
  - 积性函数与狄利克雷卷积

  否则一上来读 Min_25 的实现细节会很痛苦。
]

#exercise[
  思考：为什么 Min_25 筛里几乎总是要对
  $ floor(n / i) $
  的值离散化建表？
]
