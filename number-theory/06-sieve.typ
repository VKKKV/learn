#import "@local/math-note:0.1.0": *
#show: note-template

#set document(title: "筛法", author: "Arch User")

= 筛法

筛法用于批量预处理素数或算术函数值。
当问题不是“判定一个数”，而是“处理一整段区间里的很多数”时，筛法通常是第一反应。

== 埃拉托斯特尼筛

#definition[
  埃氏筛从 $2$ 开始，依次把每个素数的倍数标记为合数。
]

#proposition[
  埃氏筛的思想是：

  - 当前未被标记的最小数一定是素数。
  - 它的所有倍数都不是素数。
]

#remark[
  埃氏筛实现简单、常数小，适合只需要素数表的场景。
]

== 线性筛

#proposition[
  线性筛的思想是：让每个合数只被它的最小质因子筛掉一次。
  因而它能在线性时间内同时求出素数表与最小质因子。
]

#remark[
  线性筛最大的优势不是“只求素数”，而是能顺手维护很多积性函数：

  - $phi$
  - $mu$
  - 约数个数
  - 约数和
]

== 为什么线性筛重要

#remark[
  只要某个函数满足“由质因数分解递推可得”，它往往就能嵌进线性筛框架中一起预处理。
  所以在线性范围内批量求算术函数时，线性筛通常是最稳的底层模板。
]

== 例子

#example[
  用埃氏筛处理到 $20$ 时，可以得到素数表
  $ 2, 3, 5, 7, 11, 13, 17, 19 $
]

#example[
  若在线性筛中记录最小质因子，则
  $ 18 $
  的最小质因子是 $2$，
  $ 45 $
  的最小质因子是 $3$。
  这会让后续分解质因数变得更快。
]

== 使用建议

#remark[
  实战里通常这样选：

  - 只要素数表：埃氏筛够用。
  - 还要顺带求多个数论函数：优先线性筛。
  - 数据更大、需要前缀和：再考虑杜教筛、Min_25 等更高阶筛法。
]

#exercise[
  思考：若已知每个数的最小质因子，如何在均摊很低的复杂度下分解每个整数？
]
