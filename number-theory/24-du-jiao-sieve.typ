#import "@local/math-note:0.1.0": *
#show: note-template

#set document(title: "杜教筛", author: "vkkkv")

= 杜教筛

杜教筛用来快速求某些积性函数前缀和，经典对象是
$ sum_(i=1)^n phi(i) $
和
$ sum_(i=1)^n mu(i) $。
它通常出现在“线性筛不够大，但又需要多个前缀和查询”的场景里。

== 核心思想

#remark[
  杜教筛的核心思路是：

  - 利用狄利克雷卷积构造一个容易求和的关系。
  - 对前缀和进行递归拆分。
  - 配合数论分块与记忆化搜索压缩复杂度。
]

== 结构框架

#proposition[
  设希望求某个函数 $f$ 的前缀和
  $ S(n) = sum_(i=1)^n f(i) $
  若能找到另一个函数 $g$，使得
  $ f * g = h $
  且 $h$ 的前缀和容易求，则可通过卷积关系把 $S(n)$ 表示成较小规模的若干子问题。
]

#remark[
  对 $mu$ 和 $phi$ 来说，正好存在非常适合这么做的卷积恒等式，
  所以它们成为杜教筛最经典的练手对象。
]

== 为什么能变快

#remark[
  真正的加速来自两点：

  - 数论分块让
    $ floor(n / i) $
    的相同取值整段处理。
  - 记忆化让相同的子问题只算一次。

  因而虽然递归看起来很多，实际不同状态数远少于朴素枚举。
]

== 学习前置

#remark[
  想真正掌握杜教筛，前面最好已经熟悉：

  - 积性函数
  - 狄利克雷卷积
  - 莫比乌斯反演
  - 数论分块
]

== 学习建议

#remark[
  学习顺序通常建议是：

  - 先会线性筛求 $phi$ 与 $mu$
  - 再理解 $mu * 1 = epsilon$ 这类卷积恒等式
  - 再手推一次
    $ sum mu(i) $
    的杜教筛递推
  - 最后再迁移到
    $ sum phi(i) $
]

#exercise[
  思考：为什么杜教筛里几乎总会和
  $ floor(n / i) $
  的分块一起出现？
]
