#import "@local/math-note:0.1.0": *
#show: note-template

#set document(title: "类欧几里德算法", author: "vkkkv")

= 类欧几里德算法

类欧几里德算法常用于快速求和形如
$ sum_(i=0)^n floor((a i + b) / c) $
的表达式。
它的本质是把取整函数的和转化为几何或递归结构，再通过类似欧几里得算法的参数交换不断缩小问题规模。

== 典型目标

#definition[
  常见目标之一是求
  $ F(n, a, b, c) = sum_(i=0)^n floor((a i + b) / c) $
]

== 核心思想

#remark[
  它的精神与欧几里得算法类似：

  - 当 $a >= c$ 或 $b >= c$ 时，先把整除部分拆出来
  - 当 $a < c$ 且 $b < c$ 时，再通过变量对偶与取整转换递归到更小参数
]

== 为什么有效

#proposition[
  每一步递归都会让关键参数下降，尤其是模数或系数规模下降，因此总复杂度通常远好于朴素枚举。
]

#remark[
  从结构上说，这类算法是在“整数点计数”和“取整函数求和”之间来回转换。
]

== 常见应用

#remark[
  类欧常见于：

  - 取整求和
  - 某些概率/计数模型的快速统计
  - Farey 序列和有理逼近中的辅助计算
]

#exercise[
  思考：当 $a >= c$ 时，为什么
  $ floor((a i + b) / c) $
  可以先拆出一部分与 $i$ 线性相关的显式和？
]
