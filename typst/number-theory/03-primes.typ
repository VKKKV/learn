#import "../template.typ": *
#show: note-template

#set document(title: "素数", author: "Arch User")

= 素数

素数是整数乘法结构的基本单位。
像自然数在加法下由 $1$ 反复叠加一样，正整数在乘法结构下最终由素数搭建起来。

== 定义

#definition[
  大于 $1$ 的正整数 $p$ 若只有 $1$ 和 $p$ 两个正因子，则称 $p$ 为素数。
]

#example[
  $2, 3, 5, 7, 11$ 都是素数；
  $1$ 不是素数；
  $12$ 不是素数，因为它还有因子 $2, 3, 4, 6$。
]

== 算术基本定理

#theorem[
  任意整数 $n > 1$ 都能唯一地写成
  $ n = p_1^(a_1) p_2^(a_2) dots.c p_k^(a_k) $
  其中 $p_i$ 两两不同且都是素数。
]

#remark[
  这一定理说明正整数的乘法结构是“由素数唯一编码”的。
  后面讨论最大公约数、欧拉函数、约数个数和筛法时，基本都会回到这个分解式。
]

== Euclid 引理

#proposition[
  若素数 $p divides a b$，则必有
  $ p divides a $
  或
  $ p divides b $
]

#proof[
  若 $p in.not a$，则
  $ upright("gcd")(p, a) = 1 $
  由互素整除性质可知，既然 $p divides a b$，就必有 $p divides b$。
]

#remark[
  这条性质是算术基本定理成立的关键。
  它说明素数在乘法中具有不可拆分的刚性。
]

== 判定与试除

#proposition[
  判断一个正整数 $n > 1$ 是否为素数时，只需检查它是否有不超过
  $ sqrt(n) $
  的非平凡因子。
]

#proof[
  若 $n$ 是合数，则
  $ n = a b $
  其中至少有一个因子不超过 $sqrt(n)$。
  因而若所有小于等于 $sqrt(n)$ 的整数都不能整除 $n$，则 $n$ 只能是素数。
]

== 例题

#example[
  判断 $97$ 是否为素数。

  因为
  $ sqrt(97) < 10 $
  所以只需检查 $2, 3, 5, 7$。
  它们都不能整除 $97$，因此 $97$ 是素数。
]

#example[
  将
  $ 360 $
  做质因数分解：
  $ 360 = 2^3 3^2 5 $
]

== 素数为何重要

#remark[
  素数的重要性来自两点：

  - 所有整数都建立在素数分解上。
  - 许多模运算在“模素数”时结构最简单，例如费马小定理、原根、二次剩余和有限域理论。
]

#exercise[
  判断 $221$ 是否为素数。
]
