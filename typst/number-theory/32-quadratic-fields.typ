#import "../template.typ": *
#show: note-template

#set document(title: "二次域", author: "Arch User")

= 二次域

二次域是形如
$QQ(sqrt(d))$
的数域，其中 $d$ 不是平方数。
它是代数数论的入门对象，也与 Pell 方程密切相关。

#remark[
  在二次域中，整数分解、单位元、范数等概念会比整数环里更丰富。
  学习时通常从二次无理数、范数和单位群开始。
]
